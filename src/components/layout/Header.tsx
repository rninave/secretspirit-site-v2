export default function Header() {
  return (
    <header className="w-full py-4 px-8 border-b border-gray-200 flex items-center justify-between">
      <div className="text-xl font-bold">Secret Spirit</div>
      <nav>
        {/* Add navigation links here */}
      </nav>
    </header>
  );
}